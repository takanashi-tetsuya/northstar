[package]
name = "northstar-xep-0059"
version = "0.1.0"
edition = "2021"
rust-version = "1.97"
license = "AGPL-3.0-only"
description = "Capability-free XEP-0059 Result Set Management wire models, validation, builders, and pagination helpers for Northstar"
publish = false

[package.metadata.northstar-xep]
id = 59
route-ids = []
worker-ids = []

[dependencies]
northstar-xep-core = { path = "../northstar-xep-core" }
roxmltree = "0.20"
thiserror = "2.0"
serde = { version = "1.0", features = ["derive"], optional = true }

[features]
default = []
serde = ["dep:serde"]

