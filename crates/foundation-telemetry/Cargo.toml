[package]
name = "foundation-telemetry"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-only"
description = "Observability, OpenTelemetry helpers, and trace context propagation for Northstar microservices"
publish = false

[dependencies]
tracing = "0.1"
serde = { version = "1.0", features = ["derive"] }
thiserror = "2.0"
