[package]
name = "foundation-db-test"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-only"
description = "Database invariant and concurrency-model testing primitives for Northstar"
publish = false

[dependencies]
serde = { version = "1.0", features = ["derive"] }
thiserror = "2.0"
