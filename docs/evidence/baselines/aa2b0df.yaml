# Program 6 M00-07 baseline freeze. This is an observation of an exact commit,
# not a passing release, deployment, or infrastructure certification.
schema_version: "1.0"
baseline:
  commit: "aa2b0df03bed53bc6a032c5a5ac74374bb62167b"
  short_commit: "aa2b0df"
  branch: "dev"
  title: "split program-5"
  observed_at: "2026-09-04T04:41:02Z"
  recovery_plan: "NS-P6-RECOVERY-2026-09-05"

ci:
  provider: "GitHub Actions"
  workflow: "CI"
  run_id: "33833472340"
  url: "https://github.com/takanashi-tetsuya/northstar/actions/runs/33833472340"
  conclusion: "failure"
  run_attempt: 1
  jobs:
    success:
      - "Dependency licenses and sources"
      - "RustSec dependency audit"
      - "Web static checks"
    failure:
      - "Isolated disaster-recovery drill"
      - "MIX database, wire and federation validation"
      - "Pinned non-root container build"
      - "PostgreSQL protocol integration"
      - "PostgreSQL workload role boundary"
      - "Privilege-separated XEP-0487 HTTPS discovery"
      - "Rust quality gates"
      - "Two-domain S2S federation"
      - "Wire contracts (Buf)"
    cancelled:
      - "Isolated PostgreSQL and Redis invariants"
    skipped_by_workflow_condition:
      - "Scheduled 1,000-session production envelope"
      - "Scheduled multi-node cluster and raw 1,000-session load"
      - "Timed parser robustness suite"

catalog:
  source: "catalog/services.yaml"
  inventory_command: "node scripts/check-microservice-catalog.mjs"
  service_count: 51
  stanza_route_count: 38
  owned_table_count: 77
  maturity:
    planned: 36
    scaffolded: 0
    prototype: 8
    executable_prototype: 7
    integrated: 0
    production: 0
  executable_prototype_services:
    - "xmpp-edge"
    - "identity"
    - "session-directory"
    - "message-ingress"
    - "delivery-router"
    - "protocol-registry"
    - "xep-0313-mam"

authority:
  monolith:
    status: "production-baseline"
    role: "protocol behavior reference and supported single-process baseline"
  distributed_services:
    status: "prototype-program"
    integrated: false
    production: false
  multi_region:
    status: "not-production-validated"

known_blockers:
  - "Rust quality gates fail at the baseline."
  - "Wire contracts (Buf) fail at the baseline."
  - "PostgreSQL role, protocol, and disaster-recovery validations fail or are cancelled."
  - "Container, XEP-0487, MIX, and two-domain federation validations fail."
  - "The baseline lacks branch-protection and required-check enforcement evidence."
  - "Maturity-evidence schema enforcement is not present at this baseline; M01-07 owns that work."

claim_boundary:
  - "A successful local command is not evidence that a failed or cancelled CI job passed."
  - "Static catalog validation is not evidence of a working PostgreSQL, Kafka, multi-process, or multi-region deployment."
  - "This file must not be changed to promote a service; a later immutable task evidence record is required."
