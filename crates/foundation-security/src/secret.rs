//! Secret types are defined at the crate root for backwards compatibility.
//! This module is the explicit ownership boundary used by new services.
pub use crate::{OpaqueToken, SecretBytes, SecretString};
