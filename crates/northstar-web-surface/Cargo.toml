[package]
name = "northstar-web-surface"
version = "0.1.0"
edition = "2021"
rust-version = "1.97"
license = "AGPL-3.0-only"
description = "Deployment-surface capability model and deterministic dependency resolution for Northstar"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
